#include<iostream>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n,A[50]={0},res=0;
    cin>>n;
    for (int i = 0; i < n ; i++)
    {
        cin>>A[i];
    }
    for (int i = 0; i < n; i++)
    {
        
    }
    
    return 0;
}
