#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define endl '\n'
bool check(int year){
    int arr[4];
    int temp;
    for (int i = 0; i < 4; i++)
    {
        arr[i] = year % 10;
        year = year / 10 ;
    }
    
} 
int main(){
ios::sync_with_stdio(0); 
cin.tie(0);
    int year;
    cin>>year;
  
    
    return 0;
}